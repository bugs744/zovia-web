<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Join <%= spaceName %> on Zovia</title>

  <!-- OpenGraph for link previews -->
  <meta property="og:title" content="Join <%= spaceName %> on Zovia" />
  <meta property="og:description" content="You're invited to join a shared space on Zovia. Organize what matters." />
  <meta property="og:image" content="https://zovia.studio/og-default.png" />
  <meta property="og:url" content="https://zovia.studio/join/<%= inviteToken %>" />
  <meta name="twitter:card" content="summary_large_image" />

  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen;
      background-color: #fafafa;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
    }

    .container {
      background: white;
      padding: 32px;
      border-radius: 12px;
      max-width: 480px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.05);
      text-align: center;
    }

    h1 {
      font-size: 24px;
      margin-bottom: 16px;
    }

    .token {
      font-size: 22px;
      font-family: monospace;
      background: #f3f3f3;
      padding: 12px 20px;
      border-radius: 8px;
      margin: 16px 0;
      display: inline-block;
    }

    .button {
      display: block;
      width: 100%;
      padding: 12px;
      margin-top: 12px;
      border: none;
      border-radius: 8px;
      background: #476072;
      color: white;
      font-size: 16px;
      cursor: pointer;
    }

    .button.secondary {
      background: #ddd;
      color: #333;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>You're invited to join<br><strong><%= spaceName %></strong></h1>
    <p>Use this code in the Zovia app:</p>
    <div class="token" id="code"><%= inviteToken %></div>

    <button class="button" onclick="copyCode()">Copy Code</button>
    <button class="button" onclick="openApp()">Open in App</button>
    <button class="button secondary" disabled>Get Zovia (Coming Soon)</button>
  </div>

  <script>
    function copyCode() {
      const text = document.getElementById('code').innerText;
      navigator.clipboard.writeText(text);
      alert('Code copied to clipboard!');
    }

    function openApp() {
      const token = "<%= inviteToken %>";
      window.location.href = `zovia://join/${token}`;
    }
  </script>
</body>
</html>